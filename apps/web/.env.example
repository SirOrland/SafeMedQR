# URL of the deployed API server.
# In Vercel: add this as an Environment Variable (all environments).
# Locally: leave unset to default to http://localhost:4000
PUBLIC_API_BASE=https://your-api.railway.app
